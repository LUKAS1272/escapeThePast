using Godot;
using System;

public class bestFit : Label
{	public override void _Ready() {
		// Text = "Ahoj";

		// int textLength = Text.Length;
		// float maxWidth = RectSize.x;
		// float finalSize = maxWidth / textLength;

		// DynamicFont preset = (DynamicFont)Get("custom_fonts/font");
		// preset.Size = (int)finalSize;
	}
}
